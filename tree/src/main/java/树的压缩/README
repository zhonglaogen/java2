两种关于树的压缩算法的介绍。
ISX：通过3层统计结构来计算一棵树的节点信息。可以快速查找到节点位置。 
XBW：用两个数组来记录整棵树的结构，压缩后，可以直接在压缩的结构中进行树节点的搜索。


要保证树的孩子是从左向右插入的，不然无法判断孩子的顺序

ISX 压缩度更高  可以截取某一段小树，利用分治的思想，，适合线段树这样找一个区间段的数据
把树用（（））来表示，用01，代替用指针表示（四字节）

如何知道左括号，快速定位右括号的位置

LRnMbBD
左括号数目，右括号数目，最小正向值，最大正向值（从0开始，左括号+1，右括号-1）,最小逆向值，最大逆向值，叶子节点个数（完整括号）


XBW 方便还原
先序遍历
统计节点是否是最后一个节点
排序一定要是稳定的排序
得到通过1作为区分的块
、
还原的时候要根据对应位置进行还原
